---
'@gentleduck/upload': patch
---

Stabilize the published React declaration surface by giving `useUploaderActions()` an explicit return type and align the shared tsdown config package with the repo's runtime tsdown version.
