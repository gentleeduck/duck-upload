# Changelog

All package-level changes live next to each package (e.g. `packages/duck-upload/CHANGELOG.md`).
Per-package changelogs are managed by [Changesets](https://github.com/changesets/changesets).

To add a changeset:

```bash
bun run changeset
```
